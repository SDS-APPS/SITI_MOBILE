<resources>


    <style name="BlurredDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>


    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:fontFamily">@font/quicksand</item>
        <item name="android:popupMenuStyle">@style/AppTheme.PopupMenu</item>
    </style>

    <style name="AppTheme.PopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:itemBackground">@drawable/bg_menu_selector</item>
    </style>

    <style name="AutoCompleteTextViewCustomStyle" parent="@android:style/Widget.AutoCompleteTextView">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">8dp</item> <!-- Ajusta el valor de padding según tus necesidades -->
        <item name="android:popupBackground">@android:color/white</item> <!-- Ajusta el color de fondo del desplegable según tus necesidades -->
        <item name="android:dropDownVerticalOffset">-8dp</item> <!-- Ajusta la posición vertical del desplegable según tus necesidades -->
        <!-- Otros atributos que quieras modificar -->
    </style>


    <style name="LoginTextInputAutoCompleteLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="boxStrokeColor">#fff</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="colorPrimary">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>


</resources>
